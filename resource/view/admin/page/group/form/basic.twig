{% set form_item = {
        id: {
            attributes: {},
            id: 'group-id',
            name: 'group[id]',
            value: target.id,
            type: 'hidden',
        },
        title: {
            attributes: {
                type: 'text'
            },
            error: error('title', form_error),
            id: 'group-code',
            label: msg('10.2', 'Title'),
            name: 'group[title]',
            value: form_input('group.title', target.title),
            type: 'text',
        },
        mode: {
            attributes: {
            },
            error: error('mode', form_error),
            id: 'group-mode',
            label: msg('10.4', 'Mode'),
            name: 'group[mode]',
            option: form.master.mode,
            value: form_input('group.mode', target.mode),
            type: 'select_single',
        },
    }
%}

{% include 'library/form/detail.twig' with {form_item: form_item} %}
