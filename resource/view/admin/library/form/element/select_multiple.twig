<select id="{{ form_field.id }}"
        class="form-control j-select_multiple {{ form_field.class }} {{ form_field.error ? 'is-invalid' : '' }}"
        data-placeholder="Please chose ..."
        multiple="multiple",
        name="{{ form_field.form_name }}"

        {% if form_field.attributes.readonly %}
            disabled="disabled",
        {% endif %}

        {% for attribute_name, attribute_value in form_field.attributes %}
            {{ attribute_name }}="{{ attribute_value }}"
        {% endfor %}
>
    {% for value, text in form_field.option %}
        <option value="{{ value }}"
            {% if form_field.value == value %}
                selected
            {% endif %}
        >
            {{ text }}
        </option>
    {% endfor %}
</select>

{% if form_field.error %}
    <div class='invalid-feedback'>
        {{ form_field.error }}
    </div>
{% endif %}
